using System;

class Example {
    public static void Main(string[] args) {
        // If <Boolean Expression> { <statement> }
        // Absolute Value
        if (a > b) then c = 0;

        // No Conten
        if a > b { c = 0; }

        // Correct - Absolute Value
        if (a > b ) c = 0;
        
        if (a > b ) c = 0 else b = 0;

    }
}